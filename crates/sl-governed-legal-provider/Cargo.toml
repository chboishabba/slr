[package]
name = "sensiblaw-governed-legal-provider"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
default = []
live-network = ["dep:ureq"]

[dependencies]
ureq = { version = "2.10", optional = true }
